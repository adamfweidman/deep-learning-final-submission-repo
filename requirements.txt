# Deep Learning Final Project — Pixels to Predictions
# Pinned versions matching the training environment used on NYU HPC.
# Python: 3.12 (uv-managed cpython recommended on HPC; 3.10+ works for inference).

# Core
pandas>=2.0
numpy>=1.26
pillow>=10.0
pyyaml>=6.0
huggingface-hub>=0.24
imagehash>=4.3
kagglehub>=1.0

# Model / training (omit if you only run inference and the adapter is preloaded)
torch==2.11.0
transformers==4.57.6
accelerate>=0.33
peft>=0.18
safetensors>=0.4
