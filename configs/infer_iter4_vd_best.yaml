attempt_id: "2026-05-06-infer-iter4-vd-best"
level: "exploratory"
experiment:
  name: "infer-iter4-vd-best"
  question: "Run inference on iter4-vd's adapter_best (val_acc=0.8216 ep 4, NEW BEST) to write submission.csv + val_scores.json + test_scores.json."
  branch: "exploit"
model: {name: "HuggingFaceTB/SmolVLM-500M-Instruct", dtype: "bfloat16", max_tokens: 8192}
data: {data_dir: "data", train_csv: "data/train.csv", val_csv: "data/val.csv", test_csv: "data/test.csv", sample_submission_csv: "data/sample_submission.csv"}
run:
  mode: "infer"
  output_dir: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-infer-iter4-vd-best"
  seed: 42
  baseline: "always_a"
  img_size: 224
  batch_size: 32
  adapter_path: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-iter4-vd-lr4e4/adapter_best"
train: {eval_num_workers: 4}
preflight: {hf_cache_dir: "/scratch/${USER}/deep-learning-final/hf_cache", download_first: false, snapshot_only: false, offline_after: true}
resources: {}
notes: "Infer on iter4-vd's ep-4 adapter (val_acc=0.8216, NEW BEST). 90-min walltime + bs=32 (smaller) to dodge slow-node timeouts."
