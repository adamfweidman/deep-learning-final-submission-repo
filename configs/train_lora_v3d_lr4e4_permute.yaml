attempt_id: "2026-05-06-lora-3d-lr4e4-permute"
level: "exploratory"
experiment:
  name: "lora-3d-lr4e4-permute"
  question: "V_β config (best so far at 0.8073) at lr=4e-4 (E8 bundle winner): does combining the strongest training signal beat 0.85?"
  branch: "exploit"
model:
  name: "HuggingFaceTB/SmolVLM-500M-Instruct"
  dtype: "bfloat16"
  max_tokens: 8192
data:
  data_dir: "data"
  train_csv: "data/train.csv"
  val_csv: "data/val.csv"
  test_csv: "data/test.csv"
  sample_submission_csv: "data/sample_submission.csv"
run:
  mode: "train"
  output_dir: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-lora-3d-lr4e4-permute"
  seed: 46
  baseline: "always_a"
  img_size: 224
train:
  n_train: null
  n_val_eval: null
  n_test: null
  epochs: 5
  batch_size: 4
  grad_accum: 2
  lr: 4.0e-4
  warmup_ratio: 0.05
  weight_decay: 0.0
  lora_r: 8
  lora_alpha: 8
  lora_dropout: 0.05
  choice_permute: true
  eval_batch_size: 48
  eval_num_workers: 4
  train_num_workers: 4
  log_every: 25
  eval_every_epoch: true
  early_stop_patience: 1
  write_submission_csv: true
  submission_threshold: 0.78
preflight:
  hf_cache_dir: "/scratch/${USER}/deep-learning-final/hf_cache"
  download_first: false
  snapshot_only: false
  offline_after: true
resources: {}
notes: |
  V_β hit 0.8073 at ep 4 with lr=2e-4. E8 (lr=4e-4) at proxy scale was bundle winner at 0.7075. V_δ = V_β config × lr=4e-4. submission_threshold=0.78 so we only write if it beats current best.
