attempt_id: "2026-05-06-lora-3e-lr8e4-3ep"
level: "exploratory"
experiment:
  name: "lora-3e-lr8e4-3ep"
  question: "V_β config × lr=8e-4 (F1 proxy winner). F1 saturated at ep 1 so try 3 epochs with patient early stop. Does lr=8e-4 at full scale beat V_β's 0.8073?"
  branch: "exploit"
model:
  name: "HuggingFaceTB/SmolVLM-500M-Instruct"
  dtype: "bfloat16"
  max_tokens: 8192
data:
  data_dir: "data"
  train_csv: "data/train.csv"
  val_csv: "data/val.csv"
  test_csv: "data/test.csv"
  sample_submission_csv: "data/sample_submission.csv"
run:
  mode: "train"
  output_dir: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-lora-3e-lr8e4-3ep"
  seed: 47
  baseline: "always_a"
  img_size: 224
train:
  n_train: null
  n_val_eval: null
  n_test: null
  epochs: 3
  batch_size: 4
  grad_accum: 2
  lr: 8.0e-4
  warmup_ratio: 0.05
  weight_decay: 0.0
  lora_r: 8
  lora_alpha: 8
  lora_dropout: 0.05
  choice_permute: true
  eval_batch_size: 48
  eval_num_workers: 4
  train_num_workers: 4
  log_every: 25
  eval_every_epoch: true
  early_stop_patience: 1
  write_submission_csv: true
  submission_threshold: 0.78
preflight:
  hf_cache_dir: "/scratch/${USER}/deep-learning-final/hf_cache"
  download_first: false
  snapshot_only: false
  offline_after: true
resources: {}
notes: |
  Promotion based on F1 proxy result (val_acc=0.725 ep 1 vs E8's 0.7075). At lr=8e-4 the model saturates fast; 3 epochs with patience=1 lets early-stop kick in. submission_threshold=0.78 so we only write submission if it beats current 0.7767-0.8073 best.
