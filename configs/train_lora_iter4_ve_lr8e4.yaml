attempt_id: "2026-05-06-iter4-ve-lr8e4"
level: "exploratory"
experiment:
  name: "iter4-ve-lr8e4"
  question: "V_β config × lr=8e-4 (F1 proxy winner): does the highest-LR variant work at full scale, or does saturation dominate?"
  branch: "broaden"
model: {name: "HuggingFaceTB/SmolVLM-500M-Instruct", dtype: "bfloat16", max_tokens: 8192}
data: {data_dir: "data", train_csv: "data/train.csv", val_csv: "data/val.csv", test_csv: "data/test.csv", sample_submission_csv: "data/sample_submission.csv"}
run:
  mode: "train"
  output_dir: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-iter4-ve-lr8e4"
  seed: 47
  baseline: "always_a"
  img_size: 224
train:
  n_train: null
  n_val_eval: null
  n_test: null
  epochs: 3
  batch_size: 4
  grad_accum: 2
  lr: 8.0e-4
  warmup_ratio: 0.10
  weight_decay: 0.0
  lora_r: 8
  lora_alpha: 8
  lora_dropout: 0.05
  choice_permute: true
  eval_batch_size: 48
  eval_num_workers: 4
  train_num_workers: 4
  log_every: 25
  eval_every_epoch: true
  early_stop_patience: 1
  write_submission_csv: true
  submission_threshold: 0.80
preflight: {hf_cache_dir: "/scratch/${USER}/deep-learning-final/hf_cache", download_first: false, snapshot_only: false, offline_after: true}
resources: {}
notes: "V_β × lr=8e-4 / 3 epochs / 10% warmup. F1 (lr=8e-4) was proxy bundle winner; 3 epochs since F1 saturated at ep 1 (4e4 needs longer)."
