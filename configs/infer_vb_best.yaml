attempt_id: "2026-05-06-infer-vb-best"
level: "exploratory"
experiment:
  name: "infer-vb-best"
  question: "Run inference on V_β's adapter_best (val_acc=0.8073 at ep 4) to actually write submission.csv since V_β was cancelled mid-epoch-5."
  branch: "exploit"
model:
  name: "HuggingFaceTB/SmolVLM-500M-Instruct"
  dtype: "bfloat16"
  max_tokens: 8192
data:
  data_dir: "data"
  train_csv: "data/train.csv"
  val_csv: "data/val.csv"
  test_csv: "data/test.csv"
  sample_submission_csv: "data/sample_submission.csv"
run:
  mode: "infer"
  output_dir: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-infer-vb-best"
  seed: 42
  baseline: "always_a"
  img_size: 224
  batch_size: 48
  adapter_path: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-lora-3b-allmod-permute/adapter_best"
train:
  eval_num_workers: 4
preflight:
  hf_cache_dir: "/scratch/${USER}/deep-learning-final/hf_cache"
  download_first: false
  snapshot_only: false
  offline_after: true
resources: {}
notes: "V_β reached 0.8073 ep 4 then was cancelled at upd 1600/1945 ep 5; adapter_best preserved. Run infer with --mem=64G."
