attempt_id: "2026-05-06-infer-vb-v3-scoredump"
level: "exploratory"
experiment:
  name: "infer-vb-v3-scoredump"
  question: "Re-run V_β inference with the new score-dump code to capture per-row A-E logits + metadata for calibration + retrieval overlay."
  branch: "infra-fix"
model: {name: "HuggingFaceTB/SmolVLM-500M-Instruct", dtype: "bfloat16", max_tokens: 8192}
data: {data_dir: "data", train_csv: "data/train.csv", val_csv: "data/val.csv", test_csv: "data/test.csv", sample_submission_csv: "data/sample_submission.csv"}
run:
  mode: "infer"
  output_dir: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-infer-vb-v3-scoredump"
  seed: 42
  baseline: "always_a"
  img_size: 224
  batch_size: 48
  adapter_path: "/scratch/${USER}/deep-learning-final/runs/2026-05-06-lora-3b-allmod-permute/adapter_best"
train: {eval_num_workers: 4}
preflight: {hf_cache_dir: "/scratch/${USER}/deep-learning-final/hf_cache", download_first: false, snapshot_only: false, offline_after: true}
resources: {}
notes: "Re-infer V_β to capture val_scores.json + test_scores.json with new return_scores=True path."
