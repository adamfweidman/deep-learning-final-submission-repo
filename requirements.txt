# Deep Learning Final Project — Pixels to Predictions
# Pinned versions matching the training environment used on NYU HPC.
# Python 3.12 recommended; 3.10+ works for inference.

# Core
pandas==2.3.0
numpy==2.4.4
pillow==10.4.0
pyyaml==6.0.2
huggingface-hub==0.27.0
imagehash==4.3.2
kagglehub==1.0.0

# Model / training
torch==2.11.0
transformers==4.57.6
accelerate==1.1.1
peft==0.18.1
safetensors==0.4.5
